import 'package:flutter/material.dart';

class ScreenClaimToken extends StatelessWidget {
  const ScreenClaimToken({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

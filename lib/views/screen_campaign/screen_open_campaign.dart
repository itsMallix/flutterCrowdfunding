import 'package:flutter/material.dart';

class ScreenOpenCampaign extends StatelessWidget {
  const ScreenOpenCampaign({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Open Campaign"),
      ),
    );
  }
}
